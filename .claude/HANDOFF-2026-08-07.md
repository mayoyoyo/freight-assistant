# Handoff: Goodlane Freight Agent — Phases 0–4 built, finish eval + deploy
Written 2026-08-07 by the implementation session (Fable 5). Supersedes `HANDOFF-2026-08-06.md`
(still worth skimming for who Hanson is and the original traps; everything it lists as
"next" is now DONE). The operational source of truth is `.claude/status/goodlane-freight-agent.md`
— read it in full before doing anything. Do not re-litigate decided things; flag genuine
blockers to Hanson only.

## Where everything is
- **Branch topology (stacked)**: `main` → PR #1 `feat/phase1-schema-seed` → PR #2
  `feat/phase2-ingestion` → PR #3 `feat/phase3-agent-ui` → `feat/phase4-eval` (pushed, NO
  PR yet — open PR #4 "Phase 4: eval harness + report" based on phase3 branch when the
  eval completes). Hanson merges PRs in order; after each merge, retarget the next PR's
  base (GitHub usually auto-retargets stacked PRs on merge).
- **Local DB**: Postgres.app on :5432, db `freight_assistant`, seeded (`pnpm seed`).
  `.env` at repo root (gitignored) has DATABASE_URL + ANTHROPIC_API_KEY + DEEPGRAM_API_KEY.
- **WALKTHROUGH.md** (repo root): Hanson's private study doc. Gitignored — NEVER commit it,
  never mention its contents in committed files. Append a section after every phase
  (what/how/why + likely interviewer Q&A). It is current through Phase 4A/4C.
- Vercel is connected and auto-deploys previews per branch (import already done).

## SESSION-3 UPDATE (2026-08-07, later): most of the checklist is DONE
Steps 1-3 complete (CE0027 fixed+seeded; baseline COMPLETE 51/72 w/ full decision
trail; judge v2 PROMOTED 100/100/kappa-1.0). Step 4 HALF-done: 36/72 post-fix runs
generated before the raised $40 cap was re-hit; L05 verified fixed 0/3->3/3;
S01/S02 flipped missing->spurious (precision), see status doc. Luna leg measured:
pass@1 7/24 at $0.0026/query. Three grader false-fail classes found via the
cross-provider run — fixed + regression-pinned (commit 8abca6e). NEW BLOCKER:
cap needs raising again (~$55-60 total) for the remaining ~$10-12: 36 post-fix
runs, sonnet+haiku legs, Luna re-run, then report/ADRs/PR #4.
Read `.claude/status/goodlane-freight-agent.md` "SESSION 3 RESULTS" for details.

## ⛔ Blocker gating all remaining API work
Anthropic org monthly spend cap exhausted — every model returns 400 "reached your
specified API usage limits… regain access 2026-09-01". Hanson must raise the limit at
console.anthropic.com → Settings → Limits. Until then the live demo is down and the
resume checklist below cannot run. Verify access with a 1-token haiku call before
starting anything expensive.

## RESUME CHECKLIST (in order, once API access returns)
1. `pnpm re-extract --ids CE0027` → re-run `scripts/resolve.ts` → `pnpm seed`
   (fixes the 10× rate bug $280→2800; re-extract's delete→rerun path is UNEXERCISED — watch it).
2. Finish the BASELINE: `EVAL_RUN_ID=<id> pnpm eval` — 48 remaining runs (set_retrieval,
   email_draft, abstention buckets). NOTE: baseline must run the PRE-fix tool semantics —
   the fix round is already merged into feat/phase4-eval, so run the baseline from commit
   `e4ff03f` (pre-fix) in a worktree, OR accept the partial-baseline-as-baseline and
   document honestly (24 factual_lookup runs at pre-fix + failures localized). DECIDE
   EXPLICITLY and write the choice into the report — do not silently mix pre/post-fix runs
   in one "baseline". (The completed 24 runs on disk in evals/results/ ARE pre-fix.)
3. Judge: `calibrate.ts --version v2` (target commitments TPR >80%; v1 = 76.9/100) then
   `stability.ts` (repeat-stability, 6 pinned items). Update evals/judge/versions.md;
   bump JUDGE_VERSION only if v2 measures better.
4. POST-FIX full run (current branch head) → before/after table into evals/report.md
   (expect L05 3/3-fail → pass; L06/L07 are generation-mode, may persist — report honestly).
5. Model comparison: `evals/compare.ts --models claude-opus-5,claude-sonnet-5,claude-haiku-4-5,gpt-5.6-luna`
   — OpenAI leg DONE 2026-08-07 (key in .env + verified; @ai-sdk/openai installed;
   run-agent.ts routes gpt-* to the openai provider; pricing map filled with verified
   $0.20/$1.20; smoke-tested through the real tool loop). "Codex 5.6 Luna" was a
   misnomer: the model is GPT-5.6 Luna, id `gpt-5.6-luna`. Just run the sweep.
6. Fill ADR 003/004 "pending" numbers (docs/decisions/). Update WALKTHROUGH.md Phase 4B
   section (harness story, judge calibration numbers, before/after row).
7. Open PR #4 for feat/phase4-eval.
8. Phase 5 per plan: Neon attach via Vercel dashboard (Hanson does dashboard steps),
   `vercel env pull`, seed Neon, verify prod URL on the two spec queries (data/raw/README.md:91),
   README (architecture diagram, decisions table, how-to-run, eval summary, what-I'd-improve
   incl. chat persistence + name-keyed carrier lookup + Haiku-for-intake), grant reviewers
   if repo goes private (ellencbrandenberger-blip, jonhmchan, danfriedman). WALKTHROUGH
   final sections + one full add-a-tool live-extension rehearsal rep with Hanson.

## Traps learned this session (do not rediscover)
- **AI SDK is v7** (ai@7.0.54): `instructions:` not `system:`; `await convertToModelMessages`;
  `createUIMessageStreamResponse`; `onToolExecutionEnd`; `MockLanguageModelV4` from ai/test;
  useChat has no input/handleSubmit. Full drift table in WALKTHROUGH Phase 3 section.
- **claude-opus-5**: rejects temperature/top_p/top_k; thinking ON by default (maxOutputTokens
  caps thinking+text together — route+runner pinned 8192; bump if max_tokens stops appear);
  generateObject defaulted maxOutputTokens to 128000 before pinning.
- **Worktree agents may check out at the repo's FIRST commit** — always `git checkout -b
  <branch> <base>` explicitly and verify `git log` before working (bit two agents).
- **Local test flake**: first Postgres connection from cold can exceed vitest's 5s default —
  the DB-backed agent test has a 15s timeout + skipIf(no DATABASE_URL && no .env) for CI.
- Dataset ground truth (eval-critical, all verified): omission decoys (145 emails stated-MC
  with no MC in body → null is CORRECT); call_006 MC=345678 (self-corrected); call_046
  planted wrong MC 665432 (name_fuzzy recovers 663210); call_038 only truly MC-less call;
  all calls occurred_at NULL; 5 emails write "MC #N/A" (Blue Eagle); CE0027 10× rate bug
  (fix = resume step 1); "Chesapeake Haulers" is the only real ASR word error.
- Judge state: v1 measured (tone 100/100 κ1.0; commitments TPR 76.9 TNR 100 κ0.70 — 3 FPs
  diagnosed: REFERENCE_DATE unknown to judge + own-disposition treated as invented). v2
  prompt written, UNCALIBRATED, default stays v1 until measured. Full decision tree in
  evals/judge/versions.md.
- Optional 4-minute human task for Hanson: evals/components/wer-check.md (3 clips + what
  to listen for; confirms call_006/call_046 audio ground truth).

## Process rules (unchanged, graded)
Feature-branch PRs with real descriptions; conventional commits; independent review of
each PR via Codex CLI (`codex exec --sandbox read-only "review …"`) with a triage table
posted as a PR comment — verify findings against the DB/code before accepting, reject
with written rationale where wrong (see PR #1 for the pattern). Delegate heavy work to
worktree agents (opus), main session reviews + verifies claims independently. Update
WALKTHROUGH.md + .claude/status/ + auto-memory as you go. Never set temperature anywhere.
Budget note: total project API spend so far ≈ $20; remaining ≈ $30–40.
