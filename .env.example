# Copy to .env and fill in. Never commit real values.
DATABASE_URL=postgres://user:password@host/db
ANTHROPIC_API_KEY=sk-ant-...

# Only needed to re-run the offline transcription script (scripts/transcribe.ts);
# the deployed app never calls Deepgram.
DEEPGRAM_API_KEY=
